# Projects owned by github.com/cvhariharan. Same schema as data/featured.yml —
# see the field documentation there. Aggregated via config.extra.project_files.
#
# All of this owner's projects are currently featured, so they live in
# data/featured.yml instead. Add non-featured ones to the list below.

[]
